<% if flash[:notice] %>
	<div class="notice"><%= flash[:notice] %></div>
<% end %>

<%= simple_form_for @pokemon do |f| %>
  <%= f.input :name, label: "Pokemon name"%>
  <%= f.button :submit %>
<% end %>